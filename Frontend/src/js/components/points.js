export default function Point(points) {
    return `
    <section class="userGrid">
        <section class="user" id="gridItemA">
            <h2><span class="color">Points Earned:</span><span id="number"> ${points}</span></h2>
        </section>
    </section>
    `
}