export default function Available(availables) {
    return `<section class="userGrid">
    <section class="user" id="gridItemA">
    <h3>Points Available: <span id="number">${availables}</span></h3>
    </section>
    </section>
    `
}